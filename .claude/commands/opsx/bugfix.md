---
name: "OPSX: Bugfix"
description: Fast-fix a defect on a post-apply or archived OpenSpec change — skip full plan, keep validate/acceptance gates
---

Use the openspec-bugfix skill to anchor a change that has already entered apply (or is archived), classify implementation vs spec defect, apply a root-cause-first fix, and record the outcome under OpenSpec paths only.
