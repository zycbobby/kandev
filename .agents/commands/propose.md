---
name: "OPSX: Propose"
description: Start a new feature or change — create the change with schema=superpowers-bridge, then produce proposal.md and spec deltas
---

Use the openspec-propose skill. If no active change exists yet, it must create one with `openspec new change <name> --schema superpowers-bridge` before writing the proposal.
