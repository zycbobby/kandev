# Review Round 1 — fix-workflow-selector-scroll

## Status

finalized

## Trigger

post-apply（apply 完成 4 个 task 并发出 step_complete 后，用户反馈「移动端还是不行，无法滚动」）

## Checkpoint

cp-001（gitCommit 7b71da01c）

## Review Inputs

- 移动端还是不行，无法滚动

## Classification

- **A. Code-only fix** — `WorkflowSelectorRow` 的 PopoverContent 已加 `max-h + overflow-y-auto`，但移动端仍不可滚动。

## Artifact Updates Required

- [x] `tasks.md`：追加 Task 5（对齐 combobox.tsx 的 PopoverContent 接线 + 增强移动端 E2E 用真实触摸滚动）。
- [x] `design.md`：追加 D1.1（移动端接线修正 + 验证方式）。

## Implementation Tasks Added

- Task 5（`fix: make workflow selector popover scrollable on mobile`）。

## Rejected or Deferred Feedback

- 无。不采纳「改成移动端 Drawer/Sheet」——超出本缺陷范围，且 design.md 已明确 Non-Goal。

## Verification Plan

- 红：Playwright `mobile-chrome` 用真实触摸拖动（touchscreen/wheel dispatch）断言列表 `scrollTop` 变化，修复前失败。
- 绿：接线后重跑移动 + 桌面 E2E、vitest、typecheck、eslint、openspec validate --strict。

## Resume Decision

resume apply（`applyBlocked=false`）；回到 `openspec-apply-change` 实现 Task 5。
