# Re-review package Task 3 fix (base 7d89b1d77..HEAD)

## commits
89a24f5be test(sync-workflow): fail when stub startup stalls

## diff
diff --git a/scripts/sync-workflow.test.sh b/scripts/sync-workflow.test.sh
index f085a2b1e..1b330844d 100644
--- a/scripts/sync-workflow.test.sh
+++ b/scripts/sync-workflow.test.sh
@@ -107,20 +107,23 @@ server.serve_forever()
 PY
 server_pid=$!
 for _ in $(seq 1 100); do
 	if [ -s "$port_file" ]; then break; fi
 	if ! kill -0 "$server_pid" 2>/dev/null; then
 		fail "stub server starts"
 		break
 	fi
 	sleep 0.01
 done
+if [ ! -s "$port_file" ]; then
+	fail "stub server starts"
+fi
 
 if [ -s "$port_file" ]; then
 	stub_url="http://127.0.0.1:$(<"$port_file")"
 	if python3 "$ROOT_DIR/scripts/sync-workflow.py" "$stub_url" "$output_dir" >/dev/null; then
 		pass "sync-workflow exports from stub"
 	else
 		fail "sync-workflow exports from stub"
 	fi
 
 	expected_files=("kanban.yml" "pr-review.yml" "kanban--beta.yml")
