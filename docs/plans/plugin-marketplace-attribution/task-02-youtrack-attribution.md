---
id: "02-youtrack-attribution"
title: "Correct YouTrack attribution"
status: in_progress
wave: 1
depends_on: []
plan: "plan.md"
spec: "../../specs/plugins/requirements/marketplace.md"
---

# Task 02: Correct YouTrack attribution

## Acceptance

- `ahmedbally/kandev-plugin-youtrack/manifest.yaml` declares
  `author: "ahmedbally"`.
- The manifest declares
  `repo_url: "https://github.com/ahmedbally/kandev-plugin-youtrack"` and keeps
  the plugin id, capabilities, and package contract unchanged.
- A newly published release contains the corrected manifest, so the official
  catalog displays the external contributor and links to the external repo.

## Verification

From the attached YouTrack plugin worktree:

```bash
go test ./...
go vet ./...
```

Then run the repository's existing release workflow to publish a new release
containing the manifest. If the external repository cannot be attached or its
maintainer cannot publish the release, report that as the task blocker instead
of changing the Kandev registry to misrepresent ownership.

## Files likely touched

- `ahmedbally/kandev-plugin-youtrack/manifest.yaml`
- Release metadata generated by the plugin repository's existing release
  workflow, if that workflow is run as part of implementation.

## Dependencies

None.

## Parallelism

parallel-safe with task 01 because it uses a separate repository and no shared
registry files. The primary session remains sequential by default.

## Inputs

- `docs/specs/plugins/requirements/marketplace.md`, catalog attribution scenarios.
- `docs/plans/plugin-marketplace-attribution/plan.md`, external ownership and
  release requirements.
- The plugin repository's `manifest.yaml`, `Makefile`, and release workflow.

## Output contract

Report the branch/repository, exact files changed, release tag or maintainer
access blocker, commands and results, package path, and task/plan status update.

## Results

2026-08-22: Updated the external contributor fork branch
`fix/correct-marketplace-attribution` in `ahmedbally/kandev-plugin-youtrack`.
The manifest now uses version `0.1.1`, author `ahmedbally`, and
`https://github.com/ahmedbally/kandev-plugin-youtrack`. README build references
were updated for the new release version; plugin behavior and capabilities are
unchanged.

Verification passed:

- `go test ./...` (25 tests)
- `go vet ./...`

The fork cannot publish the upstream release directly. The upstream maintainer
must merge the PR and run the existing release workflow before the official
catalog can observe the corrected metadata.
