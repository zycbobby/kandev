---
spec: docs/specs/disambiguate-waiting/spec.md
created: 2026-09-04
status: implemented
---

# Implementation Plan: Waiting Attribution (parked-on-background-work)

## How this plan was produced

The card was moved directly from Spec Review round 5 ("accept the remaining gaps
and proceed to Build") into Build without a Plan step. This plan was reconstructed
from the frozen spec (`docs/specs/disambiguate-waiting/spec.md`, 46 ACs) plus a
recon pass against the current tree (main has advanced past the spec's pinned
`bf62f39b1`; no structural drift, only line-number and minor-signature drift —
see "Recon deltas" below). No spec content is changed by this plan.

Per the round-5 human decision, 21 findings (F1-F22 in the task plan's Spec Review
history) are carried into Build OPEN. This plan makes the required build-time
calls for the five NEEDS-RETHINK-class ones and the two FIX-FIRST ones the human
flagged as most load-bearing (F6 security, F7 ordering), and records them below
rather than silently building past them.

## Recon deltas from the spec's `bf62f39b1` citations (verified against current `main`-derived tree)

- `clientCapabilitiesForAgent` builds `acp.ClientCapabilities` from the vendored
  `acp-go-sdk`, not a local struct — irrelevant to this spec (elicitation's concern).
- `agent.go` action switch has grown to 12 cases; the "completeness" test only
  covers 7. Not a blocker, but the new `agent.background.probe` case must be added
  to that test explicitly since the test will not catch its omission.
- `sendStreamRequest` lives in `client_stream.go`, not `agent.go` (same `Client` type).
- `CheckSessionAccess` lives in `manager.go:409`, not `manager_interaction.go`.
- Two parallel frontend `Task` type shapes exist with different casing:
  `lib/types/http.ts` (snake_case, wire) and
  `lib/state/slices/kanban/types.ts` (camelCase, store). `rich-task-list-row.tsx`
  uses the former, `kanban-card-content.tsx` the latter. New fields must be added
  to both, plus the wire→store normalization/hydration layer.
- Mobile session-switcher file is at
  `apps/web/components/task/mobile/mobile-sessions-section.tsx`, not
  `apps/web/components/mobile/...`.
- The `CancellationPendingSnapshot` / `EnrichCancellationPending` precedent
  (`task_operations.go:4478`, `internal/task/dto/cancellation_pending.go`) is
  exactly the shape to mirror for `parked_on_background_work` + `revision`: a
  type-asserted "snapshot provider" upgrade that degrades to `revision: 0`.

## Build-time decisions for the round-5 NEEDS-RETHINK findings (F1-F5)

These are not spec changes. They are the concrete choices Build must make because
the AC text cannot be satisfied literally, recorded here per the round-5 human
decision's explicit instruction.

- **F1 (AC-69, launch-recogniser seam).** Split into two tests: (a) a behavioural
  test that registers a second recogniser through the public registration API and
  asserts the resulting session parks and its task row renders
  `task-state-background-running` (this test may import whatever it needs —
  projection, rendering, test harness); (b) a package-graph import-direction test
  asserting the recogniser-registry package imports nothing from the probe,
  projection, or rendering packages. (b) is the mechanically-checkable form of the
  "adding a vendor changes nothing else" guarantee; (a) is the observable behaviour.
  See task-02.
- **F2 (AC-35, architecture test).** Not package-granularity (the projection lives
  in `internal/orchestrator`, the same package as the `claudeBackgroundPromptHandoff`
  accessor, per precedent). Instead: a source-scoped test naming the exact new files
  this feature adds (parked-projection, probe client, recogniser registry — listed
  explicitly in task-05/task-06) and asserting none of their source text references
  `claudeBackgroundPromptHandoffEnabled` or `features.claudeBackgroundPromptHandoff`.
  This is checkable by construction and does not vacuously pass by naming an
  unrelated package. See task-05.
- **F3 (AC-27b / AC-70 / AC-72, truncated vs. exact turn-start boundary).** The
  probe predicate, D5, and AC-80 all require comparing against the *truncated*
  turn start. AC-27b/AC-70/AC-72's GIVEN text says "strictly before the recorded
  turn start" (untruncated). Build implements the truncated comparison everywhere
  (it is the only self-consistent reading and the one AC-80 requires) and treats
  the three ACs' prose as describing the truncated boundary. See task-03.
- **F4 (AC-77, epoch reset has no publish trigger).** Chose: **the boot payload is
  the sole reset-delivery mechanism.** A client that reconnects the WebSocket
  without re-fetching boot will not receive a synthetic reset frame — no generic
  "resync on reconnect" broadcast mechanism exists in this codebase today, and
  building one is a separate architectural change outside this feature's scope.
  This narrows AC-77's WebSocket-only clause; the gap is accepted per the round-5
  note's explicit alternative ("AC-77's WebSocket-only clause is withdrawn"). See
  task-06.
- **F5 (`parked_epoch` non-monotonic across a clock step).** Kept as specified
  (Unix-nanosecond process start time). The risk (NTP step / restored snapshot /
  host migration producing an equal-or-lower epoch) is accepted per the round-5
  note's explicit allowance to "record the accepted risk" — building a persisted
  monotonic counter is a durability change this feature's "no schema migration"
  constraint forecloses. See task-06.

## Other Build-time calls (FIX-FIRST findings the human flagged as load-bearing)

- **F6 (security).** `agent.background.probe` MUST NOT ride the same unguarded
  path as `RespondToPermissionBySessionID`. Add an explicit
  `CheckSessionAccess`-equivalent guard on the new WS action before it reaches
  agentctl. See task-04.
- **F7 (ordering).** `session.turn_finished` is emitted **before** the synchronous
  probe is taken, so AC-76's "not delayed" holds unconditionally. See task-03/task-05.
- **F8/F10/F12 (cheap, closed here).** Linux boot anchor named as `/proc/uptime`
  (relative monotonic offset from process start, not `/proc/stat` `btime`); a
  non-positive `KANDEV_PARKED_PROBE_INTERVAL` is rejected at config load the same
  way as the budget; the five process-tree ACs (AC-70/70a/71/72/80) run as Go
  tests in `internal/agentctl/.../probe` guarded by `runtime.GOOS`, with the
  Darwin branch skipped in Linux CI (no macOS runner exists) and exercised only
  when run locally on `darwin`. See task-03.
- **F18 (contract amendment).** `docs/specs/platform/background-work-liveness.md:25`
  amendment is task-08, not left implicit.

## Implementation Waves

Ordered so each wave is independently testable against the `BackgroundProbe`
port / recogniser registry, per the spec's own "Notes for implementation"
guidance (land the transport before the projection; build the projection
against the port, not the real probe).

- [x] [Task 01: `turn_started` stream event and `observed_detached` clearing](task-01-turn-started-event.md) — commit `0c2253717`. Implemented as agentctl `EventTypeTurnStarted` (emitted from `beginPromptTurn`, covers both human and synthetic-wakeup dispatch) plus a backend `observedDetached` map on `orchestrator.Service`, set from the existing `normalizedIsDetachedLaunch` shell-kind branch in `trackBackgroundToolUpdate` and cleared on the new event in `handleAgentStreamEvent`. All new/updated tests green; `go build ./...`, `go vet ./...`, `golangci-lint run --new-from-rev=bf62f39b1` clean.
- [x] [Task 02: Launch-recogniser registry seam](task-02-recogniser-registry.md) — commit `2eb1aba`. Added `backgroundlaunch` (new leaf package: `Recognizer` interface, `Register`/`Lookup`/`RecognizesDetachedLaunch`, panic-on-nil/empty/duplicate registration, fail-closed on a panicking recognizer). Registered Claude's recognizer via `init()` in `background_launch_recognizer.go`; rewrote `stampBackgroundShellWork` to delegate to the registry. AC-69(a) (behavioural: a second registered agent attests through the unmodified `stampBackgroundShellWork`) and AC-69(b) (import-direction: registry package imports nothing from orchestrator/task/probe/apps-web) both covered. `go build ./...`, `go vet`, full `acp`+`orchestrator` suites, `golangci-lint run --new-from-rev=bf62f39b1` all clean.
- [x] [Task 03: Background-workload liveness probe (agentctl, cross-platform)](task-03-liveness-probe.md) — commit `52e0e4411`. New package `internal/agentctl/server/process/probe`: `ProbeBackgroundWorkloads(agentPID, turnStart)` returns `live`/`settled`/`unknown` (D5/D9). Transitive-descendant closure + start-time predicate computed against an injectable `processTableReader` seam; Linux reader parses `/proc/<pid>/stat` anchored to `/proc/uptime` (not `btime`, per F8), Darwin reader uses `sysctl kern.proc.all` (already wall-clock); Windows has no reader and always returns `unknown`. Turn start is truncated to the platform resolution before comparison, everywhere (F3). Synthetic table tests cover AC-27a/70/70a/71/72/80 deterministically; `probe_realtree_test.go` re-drives the same ACs against this test binary's real subprocess tree, GOOS-gated at runtime (compiles everywhere, explicit skip in CI) — verified passing for real on this session's Darwin host, including the own-process-group case (AC-71) and the truncation case on Linux only (AC-80; Darwin's 1µs resolution has no realistic margin for a real-tree assertion, so AC-80 is authoritatively covered by the synthetic test there). `go build`/`go vet` clean cross-compiled for linux/darwin/windows; full `probe` suite green (incl. `-race`); `golangci-lint run --new-from-rev=bf62f39b1` clean. Pre-existing, unrelated GitLab-CLI test failures observed elsewhere in the `process` package (confirmed outside this diff's scope).
- [x] [Task 04: Probe transport (`agent.background.probe` WS action) and config](task-04-probe-transport.md) — commit `cb2bf4398`. Added the `agent.background.probe` WS action on agentctl (`handleWSBackgroundProbe`), gated on the optional `adapter.TurnStartRecorder` capability interface (task-01's `RecordedTurnStart`) and `process.Manager.AgentPID()`; wired end-to-end through `agentctl-client.Client.ProbeBackgroundWorkloads` (exhaustive failure→`ProbeResultUnknown` mapping), `lifecycle.Manager.ProbeBackgroundWorkloadsBySessionID`, the `executor.AgentManager` interface, `backendapp.lifecycleAdapter`, to a new `orchestrator.Service.ProbeBackgroundWorkloads` production `BackgroundProbe` port implementation. F6 (round-5, security): added an explicit `authorizeSession` guard before the call reaches the executor chain (the spec's claimed inherited guard doesn't exist in the tree) and pinned it in `session_scope_matrix_test.go`. `KANDEV_PARKED_PROBE_BUDGET`/`KANDEV_PARKED_PROBE_INTERVAL` config added with non-positive-budget rejection (default fallback + warn log) and F10's zero-interval-is-valid/negative-interval-rejected distinction; budget applied via `context.WithTimeout` around the executor call, never baked into the transport. `executor.ProbeResult` re-exports `client.ProbeResult` so `internal/orchestrator` never imports `internal/agent/runtime/agentctl` directly (`ARCH-RUNTIME-IMPORT` architecture-lint gate — caught and fixed post-hoc, not baselined). All new unit tests green across 3 packages; `go build ./...`, `go vet ./...` (incl. cross-compiled linux/darwin/windows for touched shared code), `golangci-lint run --new-from-rev=bf62f39b1`, and `python3 scripts/lint-architecture.py --all` all clean. Broad regression run across `orchestrator`/`agentctl`/`agent`/`backendapp`/`integration` showed only the two already-known pre-existing/environmental failures (GitHub-CLI login-shell shim depending on this host's `~/.bash_profile`; GitLab-CLI remote-host tests) — no new regressions.
- [x] [Task 05: Parked projection (backend, session-level, `BackgroundProbe` port)](task-05-parked-projection.md) — commit `043c51ca0`. New `internal/orchestrator/parked_projection.go`: per-session `parkedSessionState` (parked, revision, lastSample, lastSampledAt) behind `parkedMu`, mirroring `CancellationPendingSnapshot`'s single-critical-section pattern; `ParkedSnapshot`/`ParkedEpoch` public accessors for task-06. Wired via one chokepoint — `onSessionStateChangedForParkedProjection`, called from `updateTaskSessionStateWithHook` (every session-state transition passes through it) — rather than each of `setSessionWaitingForInput`'s ~40 call sites individually: entering `WAITING_FOR_INPUT` triggers D2's synchronous first sample (`settleParkedProjectionSync`, skipped entirely when `observed_detached` is false per AC-40a); leaving it triggers D8's immediate clear with no new sample (`clearParkedOnSessionStateLeft`, AC-68). F7 ordering: `updateTaskSessionStateWithHook` runs downstream of `completeTurnForTaskSession` in the real turn-settle call path, so the probe always follows whatever publishes `session.turn_finished`; pinned by a source-text ordering test (same technique as F2, since a runtime reordering test would need the full task-service/notification wiring) rather than left implicit. F2's AC-35 architecture test added as `parked_projection_no_flag_test.go`, file-scoped per the round-5 disposition. Sampling loop (AC-53/54/D9): one goroutine per parked session on `KANDEV_PARKED_PROBE_INTERVAL` (interval `<=0` never starts a loop), lifecycle mirrors the existing `sendNowCtx`/`sendNowCancel`/`sendNowWorkers` pattern and is joined in `Service.Stop()`. F9 (round-5): the periodic tick re-reads session state fresh and discards a sample if the session's revision moved while the probe was in flight (a self-resume racing the sample), tested explicitly. `BackgroundProbe` port defaults to a thin adapter over `Service.ProbeBackgroundWorkloads` (task-04), overridable via `SetBackgroundProbe` for tests — every new test scripts the port rather than depending on the real transport or process walk, per the task's own guidance to build against task-04's port without depending on task-03's real probe. `publishParkedTransition` emits `task_session.activity_changed` (wire: `session.activity_changed`) with `parked_on_background_work`/`revision`/`parked_epoch` for AC-68; the conditional `task.updated` publish and full DTO/boot-payload wiring are task-06's. 16 new tests, all green (incl. `-race` and the package's `goleak.VerifyTestMain`); `go build ./...`, `go vet ./...`, `golangci-lint run --new-from-rev=bf62f39b1` clean. A same-session broad multi-package regression run hit host-level `no space left on device` linker failures (disk at 98%, unrelated build artifacts) on packages this task never touched; `internal/orchestrator` alone (the only package this task modified) was re-verified green in isolation, including with `-race`, both before and after that disk exhaustion. Also fixed in this commit: `docs/plans/disambiguate-waiting/plan.md`'s task-04 checkbox, edited during task-04 but never actually staged/committed at the time.
- [x] [Task 06: Task-level projection, revision epoch, API surface publish rules](task-06-task-projection-api-surface.md) — commit `c2e8b6b06`. Task-level aggregation lives in `internal/orchestrator/parked_projection.go` alongside the session-level state from task-05: `taskParkedState{sessions map[string]bool, parked, revision}` keyed by task ID, guarded by the same `parkedMu` critical section as `parkedStates` (computing the OR needs to read every tracked session of a task in one section). `recomputeTaskParkedLocked` maintains the OR incrementally — a session's flip adds/removes it from the task's `sessions` set, `len(sessions) > 0` is the aggregate, and the task's own `revision` increments only when that aggregate itself flips (AC-49: proven by a two-session test where S1 round-trips through revision 2 while S2's first-ever park only advances the task's revision to 3, never `max(2,1)`). `TaskParkedSnapshot(taskID)` is the public accessor, mirroring `ParkedSnapshot`; `ParkedEpoch()` is shared with the session-level carrier (one process-wide epoch, from task-05). `publishTaskParkedTransition` loads the task via `s.repo.GetTask` and forwards to the existing `TaskEventPublisher.PublishTaskUpdated` — no new task.updated variant needed, since the orchestrator's locked recompute already knows definitively whether the aggregate changed (unlike `publishTaskActivityIfChanged`'s own recompute-and-dedup, which exists because its callers don't know in advance). Wired from both `applyParkedTransition` (the synchronous settle/clear path) and `sampleParkedSessionTick` (the periodic loop), each only when the *session-level* recompute reported a change (AC-78: a second already-redundant session parking still gets its own `session.activity_changed`, but skips the task-level call entirely since there is nothing to recompute). DTO fields added to `TaskDTO` (`parked_on_background_work`, `parked_revision`, `parked_epoch`) and to `TaskSessionDTO`/`TaskSessionSummaryDTO` (`parked_on_background_work`, `revision` — deliberately not `parked_revision` on the session carrier, an accepted naming inconsistency per F20 — and `parked_epoch`), all always-serialized (no `omitempty`) so a settled/reset value clears stale client state. New `internal/task/dto/parked_projection.go`: `ParkedProvider`/`TaskParkedProvider` interfaces plus `EnrichParkedProjection(Summary)`/`EnrichTaskParkedProjection`, following `cancellation_pending.go`'s pattern exactly. Enrichment wired at every existing `EnrichCancellationPending(Summary)`/`EnrichTaskForegroundActivity` call site 1:1 (verified by call-count diffing): `boot_state.go` (quick-chat sessions, task list, task detail sessions), `task_http_handlers.go`, `task_ws_handlers.go`, `message_handlers.go`, plus the task-level enrichment threaded through `buildTaskDTOsWithSessionInfo`'s new `taskParkedProvider` parameter (`task_handlers.go`, `workflow_handlers.go`). The WS `task.updated` event payload (a hand-built map in `internal/task/service/service_events.go`, not the `TaskDTO` struct) gains its own narrow duplicate interface `service.TaskParkedProvider` — dto depends on task/service (see `converters.go`), so task/service cannot import dto's identical interface without a cycle; this mirrors the existing `ForegroundActivityProvider` duplication between the two packages. `SetTaskParkedProvider(orchestratorSvc)` wired at startup in `internal/backendapp/orchestrator.go` (task service) and `internal/backendapp/helpers.go` (`WorkflowHandlers`), alongside the existing `SetForegroundActivityProvider` calls. 6 new orchestrator tests (AC-38/49/50/62/78/36, `-race` clean, no `goleak` regressions), 6 new DTO tests (default-serialization, enrich/clear/nil-safety), 3 new task-service tests (WS payload field presence/absence/explicit-false). AC-39 and AC-77's consumer-side/WS-reconnect discard behavior are frontend concerns deferred to task-07/08, noted explicitly in the orchestrator test file's section comment rather than left silently uncovered. `go build ./...`, `go vet ./...`, `golangci-lint run --new-from-rev=043c51ca0`, `python3 scripts/lint-architecture.py --all` all clean; full `-race` suites green for `orchestrator`, `task/dto`, `task/service`, `task/handlers`, `backendapp`.
- [x] [Task 07: Frontend rendering — two task-icon resolvers and the board](task-07-frontend-task-rendering.md) — commit `f7a444873`. Both independent task-icon resolvers (spec §M) now special-case a parked-on-background-work signal, mirroring the existing `TASK_INTERRUPTED_ICON` sentinel pattern. Resolver A (`TaskStateIcon`, private to `apps/web/components/task/task-item.tsx`, used by the sidebar) gained a `parkedOnBackgroundWork` prop threaded from `TaskItemProps` through `TaskItem`, checked in a new precedence branch (`renderPendingOrActiveTaskIcon`, extracted to hold cyclomatic complexity down) after `foregroundActivity === "background"` and before the question-icon fallback. Resolver B (`getTaskStateIconConfig`/`getTaskStateIcon` in `apps/web/lib/ui/state-icons.tsx`, used by the board and other surfaces) gained a matching `TASK_PARKED_ICON` sentinel and a `parkedOnBackgroundWork` option on `TaskStateIconOptions`, with the same precedence extracted into `getPendingOrActiveTaskIcon`. `BackgroundWorkTaskIcon` (the tooltip-carrying, `data-testid="task-state-background-running"` violet spinner, reusing the existing `task:backgroundWorkIsRunning` i18n key — no new copy, satisfying AC-82) was promoted from a private `task-item.tsx` component to a shared export in `state-icons.tsx`, unchanged internally, so both resolvers render the identical component; `getTaskStateIcon` special-cases `config === TASK_PARKED_ICON` to render it, exactly mirroring the existing `TASK_INTERRUPTED_ICON` special case. The board (`apps/web/components/kanban-card-content.tsx`'s `renderTaskStatusIcon`) reads `task.parkedOnBackgroundWork`, bypassing both its early-return-null guard and its generic-spinner short-circuit so the parked affordance is never masked (AC-58). Rendering precedence (AC-34): pending-input (permission/clarification) and any live `foregroundActivity` always outrank parked in both resolvers and on the board; parked never overrides them. Deliberate scope pull-forward, documented per the task file: added `parkedOnBackgroundWork?: boolean` to the `Task` interfaces in `apps/web/components/kanban-card.tsx` and `apps/web/lib/state/slices/kanban/types.ts` — only the single boolean the board needs to compile; the `revision`/`parked_epoch` fields and wire-to-store hydration/merge (needed for AC-39/AC-77's stale-update discard) remain task-08's scope as originally planned. Split `TaskItemTrailingActions`/`DiffStatsRight`/`SubtaskToggle`/`TaskMenuButton` out of `task-item.tsx` into a new `apps/web/components/task/task-item-trailing-actions.tsx` to keep the file under the 600-line ESLint limit after the new precedence branch. New/extended tests: `task-item.test.tsx` (new `"TaskItem parked-on-background-work indicator"` describe — AC-23 renders the affordance, AC-34 pending-clarification/permission/generating-activity all outrank it), `state-icons.test.tsx` (new `"getTaskStateIcon — parked on background work"` describe — accessible rendering, false-default no-regression, AC-34 precedence, outranks a stale `WAITING_FOR_INPUT` coarse state), `kanban-card-status-icon.test.tsx` (new `"renderTaskStatusIcon — parked on background work"` describe — AC-58 against both early-return and spinner-short-circuit bypasses, explicit-false regression check, AC-34 precedence). `pnpm run typecheck`, `pnpm run lint` (`--max-warnings 0`), `pnpm run i18n:ratchet`, and the full affected-file Vitest suite (state-icons/task-item/kanban-card-status-icon/kanban-card-content plus a broader sidebar/kanban/task-switcher regression sweep, 117+102 tests) all green.
- [x] [Task 08: Frontend rendering — `/tasks` row, session switcher, TS types, docs amendment](task-08-frontend-session-rendering-docs.md) — commit `f400e97dd`. `/tasks` row (`rich-task-list-row.tsx`'s `PrimaryTaskLine`) threads `task.parked_on_background_work` (snake_case, matching this file's `Task` type — round-5 F19) into resolver B's `getTaskStateIcon` (AC-73a). Session switcher: `getSessionStateIconConfig`/`getSessionStateIcon` in `state-icons.tsx` converted to an options-bag signature (`state`, `className`, `options`) — the plain 6th positional param would have exceeded the repo's `max-params: 5` ESLint limit — with a new `parkedOnBackgroundWork` option and a `getPendingOrActiveSessionIcon` precedence helper (mirrors resolver A's `getPendingOrActiveTaskIcon`, needed to keep cyclomatic complexity under 15). Wired at all three call sites (`sessions-dropdown.tsx`, `session-reopen-menu.tsx`'s `shouldShowReopenStateIcon` + icon call, `mobile-sessions-section.tsx`'s `StateBadge`). Also fixed `sessions-dropdown.tsx`'s `sessionStatusTooltip` helper, which had no parked branch at all — the icon would have shown the background spinner while the tooltip text still said "Waiting for input"; added the matching precedence branch and reused the existing `task:sessionStatusBackgroundRunning` key. TS types: `parked_on_background_work`/`parked_revision`(task)/`revision`(session, per F20's accepted naming split)/`parked_epoch` added to `lib/types/http.ts`'s `Task`/`TaskSession`, `lib/types/backend.ts`'s `TaskEventPayload`/`TaskSessionActivityChangedPayload` (confirmed via Go recon that `TaskSessionStateChangedPayload` does *not* carry these fields on the real wire event, despite the task file's citation — a recon delta), and `lib/state/slices/kanban/types.ts`'s store `KanbanState` task shape. Found and fixed two latent gaps while wiring the merge layer: (1) task-07 had added the parked fields to `map-task.ts`'s `TaskLike` type but never actually mapped them into `toKanbanTask`'s return object, so the board's `parkedOnBackgroundWork` would always have read `undefined` regardless of what the backend sent; (2) the existing `applyForegroundActivity` handler in `agent-session.ts` unconditionally defaulted a missing `foreground_activity` to `null`, which would have silently clobbered a live busy signal every time task-05's parked-only `session.activity_changed` payload (which omits `foreground_activity` entirely — a distinct payload shape sharing the same wire event as the pre-existing ADR-0049 flip) arrived — fixed via a conditional-preserve pick-helper. New `mergeParkedProjection` (session-slice, session-level `revision`/`parked_epoch` carrier) and `mergeTaskParkedFields` (tasks.ts, task-level `parked_revision`/`parked_epoch` carrier) both implement the (parked_epoch, revision) lexicographic stale-discard rule from task-06's spec D1, mirroring the existing `mergeCancellationProjection` precedent with an added epoch tie-break. AC-75 confirmed by inspection + a state-slice test: the queue actions (`setQueueEntries`/`removeQueueEntry`/`setQueueLoading`/`clearQueueStatus`) only ever touch `draft.queue.*` and never the parked/foreground fields, so a queued-but-unadmitted prompt cannot locally clear the rendered affordance — the backend's task-05 D8 table alone decides. AC-76 confirmed clean: `git diff main...HEAD` for this whole card touches nothing under `internal/notifications/` or `apps/web/lib/notifications/`. Docs amendment: `docs/specs/platform/background-work-liveness.md:25`'s settled-session bullet now states a settled session may remain visually busy only when a positive out-of-band liveness sample supports it, never on registration alone; `docs/specs/INDEX.md`'s `waiting-attribution` row left as `draft` (task-09 still pending). Also split four helper clusters into new sibling modules (`session-merge.ts`, `session-environment-sync.ts`, `session-activity.ts`, `task-merge.ts`) purely to keep `session-slice.ts`/`agent-session.ts`/`tasks.ts` under the repo's 600-line ESLint limit after the new merge logic — no behavior change, all call sites re-import from the new locations. New/extended tests: `map-task.test.ts` (HTTP/WS parity for the three new fields, own describe block to stay under the 100-line-per-function limit), `session-slice.upsert.test.ts` (`"parked-on-background-work revision ordering"` — stale-revision/stale-epoch/newer-epoch-wins/omitted-field-preserved, plus the AC-75 queue-actions-don't-clobber test), `agent-session.test.ts` (`"session.activity_changed handler — parked-only payload shape"` — the clobber-bug regression + parked-field application), `tasks-activity.test.ts` (task-level parked projection — apply/stale-revision/stale-epoch/newer-epoch), `state-icons.test.tsx` (`"getSessionStateIcon — parked on background work (AC-51/52)"` — icon parity with live background, pending-input precedence, terminal-state guard, false-default no-regression), `tasks-list-view.test.tsx` (`"TasksListView row — parked on background work (AC-73a)"`), `sessions-dropdown.test.ts` (tooltip parked branch + precedence), `session-reopen-menu.test.tsx` (`shouldShowReopenStateIcon`'s parked branch), `mobile-sessions-section.test.tsx` (parked-on-background-work describe, split out to stay under the function-length limit). `pnpm run typecheck`, `pnpm run lint` (`--max-warnings 0`, iterated to zero after extracting helpers/splitting tests for the file-length/function-length/complexity/duplicate-string limits), `pnpm run i18n:ratchet`, and a broad regression sweep (`lib/ws/handlers`, `components/task`, `components/kanban-card.test.tsx`, `app/tasks`, `lib/kanban`, `lib/ui/state-icons.test.tsx`, `lib/state/slices/session`, `lib/state/slices/kanban` — 337 files, 2952 tests) all green.
- [x] [Task 09: E2E coverage](task-09-e2e.md) — commit `8a99f5844`. New `internal/office/testharness/background_probe.go`: `ScriptedBackgroundProbe` (per-session scripted `[]executor.ProbeResult` sequence, hold-at-last-value once exhausted, mirroring the existing `spyBackgroundProbe` test-double shape), wired into `RegisterRoutes` (new `orchestratorSvc *orchestrator.Service` param) behind the existing `officetestharness.Enabled()` (`KANDEV_E2E_MOCK=true`) gate as `POST /api/v1/_test/background-probe` (`{session_id, results: ["live"|"settled"|"unknown", ...]}`), calling `Service.SetBackgroundProbe` so the route's probe becomes the live one for the whole process. New mock-agent command `/parked-fixture [settleDelay]` (`cmd/mock-agent/handler.go`) emits a shell-kind `execute` tool call with `run_in_background: true` in its raw input and completes it — the one condition `trackBackgroundToolUpdate` recognises as `BackgroundWorkKindShell` and attests via `setObservedDetachedLaunch` (spec D7's shipped Claude recogniser, `internal/agentctl/.../background_launch_recognizer.go`) — after an initial `settleDelay` sleep (default 3s) so the spec has a deterministic window to read the session ID and script the probe before `settleParkedProjectionSync`'s synchronous first sample (D2) fires at turn-settle. Two Playwright specs: `parked-background-work.spec.ts` (AC-73/62/68 — desktop sidebar affordance renders at settle, clears on a `settled` sample via WS with no reload, and clears immediately on leaving `WAITING_FOR_INPUT` with no new probe sample) and `mobile-parked-background-work.spec.ts` (AC-51/34 — session-switcher row shows the parked icon, a concurrent pending clarification outranks it). Both opt into `KANDEV_MOCK_PROVIDERS=claude-acp` (the established `office-routing-*`/`mobile-tasks` pattern) since the shell-kind recogniser is registered only for agent ID `claude-acp` — the default e2e `mock-agent` profile has no registered recogniser and can never attest — and `KANDEV_PARKED_PROBE_INTERVAL=1s` so the periodic sample doesn't need a 30s+ real wait. Debugging traced two backend-side false leads (wrong ACP tool-call shape, wrong agent identity) before confirming via temporary diagnostic logging that the backend pipeline (attestation → sync probe sample → `applyParkedTransition` → `task_session.activity_changed` publish) was already correct; the actual bug was a **frontend wiring gap**: task-07/08 had already wired `parkedOnBackgroundWork` onto the kanban `Task` record and implemented the sidebar's icon-precedence branch in `TaskItem`, but nothing ever threaded the field from the store into the sidebar's data pipeline — `TaskSwitcherItem` (`task-switcher-types.ts`), `buildSidebarItem` (`task-session-sidebar-item.ts`), and the `<TaskItem>` call site in `task-switcher-row.tsx` all silently dropped it, so the already-correct icon logic never received the prop. Fixed by adding the field to `TaskSwitcherItem`, forwarding it through `buildSidebarItem`, and passing it in `task-switcher-row.tsx`. Separately discovered mid-authoring that the desktop chat toolbar's session switcher (`sessions-dropdown.tsx`, testid `sessions-dropdown-trigger`) is unreachable from the primary Dockview session view — every caller of the shared chat toolbar sets `hideSessionsDropdown` — so AC-51/34 targets the mobile session-switcher sheet (`mobile-sessions-section.tsx`, already correctly wired since task-08) instead, in its own `mobile-*.spec.ts` file so Playwright's `mobile-.*\.spec\.ts` project match runs it under the Pixel 5 device profile; added `sessions-dropdown-trigger`/`session-row-${id}` testids to `sessions-dropdown.tsx` regardless, since the desktop component itself is still directly correct and may be reachable from other call sites. Real process-tree ACs (AC-70/70a/71/72/80) remain out of scope per the task file, covered by task-03's Go tests. New Go tests: 5 for `ScriptedBackgroundProbe` (TDD, written before the implementation) plus `TestBackgroundProbeRouteNotMountedWithoutOrchestrator` (nil-orchestrator 404 guard). New frontend test: `task-session-sidebar-item.test.ts`'s two new cases for the `buildSidebarItem` wiring fix. `go build ./...`, `go vet`, `golangci-lint run ./...` (0 issues), and targeted `go test` for every touched package (`cmd/mock-agent`, `internal/office/testharness`, `internal/backendapp`, full `internal/orchestrator`) all green; `pnpm run typecheck`, `pnpm run lint` (`--max-warnings 0`), `pnpm run i18n:ratchet`, and the targeted Vitest sweep (task-item/task-switcher/session-reopen-menu/sessions-dropdown/mobile-sessions-section/state-icons/map-task/kanban-card-status-icon/task-session-sidebar-item — 187 tests) all clean. Both new e2e spec files verified green across repeated runs (4/4 and 1/1). A full-repo `make test` and a full `pnpm vitest run` were attempted but abandoned as infeasible in this sandbox (host disk repeatedly filled from the Go build cache mid-run — recovered twice via `go clean -cache`; the full frontend suite separately hit an unrelated, pre-existing runaway `ECONNREFUSED :3000` retry loop in an unrelated plugin-host test) — targeted, scoped verification of every touched package/file was used instead, consistent with task-05's prior disk-exhaustion precedent in this same plan.

## Definition of done (whole card)

`make fmt`, `make typecheck test lint`, `make lint-format`, `cd apps/web && pnpm
run i18n:ratchet` all clean; every AC in the spec has a named test (or is recorded
above as a documented Build-time reinterpretation); `docs/specs/INDEX.md` and
`docs/specs/platform/background-work-liveness.md` updated.
